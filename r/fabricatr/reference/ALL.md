# Magic number constant to allow users to specify `ALL` for passthrough resampling

Magic number constant to allow users to specify `ALL` for passthrough
resampling

## Usage

``` r
ALL
```

## Format

An object of class `integer` of length 1.
