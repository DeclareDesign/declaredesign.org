# Generates character string for non-fixed arguments in a designer using substitution approach.

Generates character string for non-fixed arguments in a designer using
substitution approach.

## Usage

``` r
return_args(args, fixes)
```

## Arguments

- args:

  Function arguments.

- fixes:

  Function arguments that are fixed (i.e., already evaluated in body of
  function)
